---
phase: 03-rag-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/rag/__init__.py, src/rag/ingest.py]
autonomous: true

must_haves:
  truths:
    - "Code files can be ingested and split at function/class boundaries"
  artifacts:
    - path: "src/rag/ingest.py"
      provides: "Semantic code chunking at function/class boundaries"
      contains: "PythonSplitter, CodeChunk, split_file(), split_directory()"
    - path: "src/rag/__init__.py"
      provides: "RAG module exports"
      exports: ["CodeChunk", "split_file", "split_directory"]
  key_links:
    - from: "src/rag/ingest.py"
      to: "python_semantic_splitter"
      via: "import and config"
      pattern: "from python_semantic_splitter import"
---

<objective>
Set up RAG module with semantic code chunking at function/class boundaries

Purpose: Enable code-aware document ingestion that preserves complete semantic units (functions, classes) rather than arbitrary text splits
Output: src/rag/ directory with ingest.py for semantic code chunking
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-rag-pipeline/03-RESEARCH.md
@.planning/phases/02-api-gateway-multi-model/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RAG dependencies and create module structure</name>
  <files>pyproject.toml, src/rag/__init__.py</files>
  <action>
    1. Add to pyproject.toml dependencies:
       - sentence-transformers>=3.0.0
       - faiss-cpu>=1.8.0
       - python-semantic-splitter>=1.0.0
       - numpy>=1.26.0
       - tiktoken>=0.5.0
    2. Create src/rag/ directory with __init__.py
    3. Export: CodeChunk (dataclass with code, metadata, chunk_type), split_file, split_directory
    4. Run: pip install -e ".[dev]" to install new dependencies
  </action>
  <verify>grep -q "sentence-transformers" pyproject.toml && python -c "from src.rag import CodeChunk"</verify>
  <done>Dependencies installed, RAG module imports without error</done>
</task>

<task type="auto">
  <name>Task 2: Implement semantic code chunking (function/class boundaries)</name>
  <files>src/rag/ingest.py</files>
  <action>
    Create src/rag/ingest.py with:
    
    1. CodeChunk dataclass: code (str), metadata (dict), chunk_type (str), file_path (str)
    2. split_file(file_path: str, max_chunk_tokens: int = 800) -> List[CodeChunk]:
       - Use PythonSplitter with SplitterConfig(
           max_chunk_size=1500,
           min_chunk_size=100,
           preserve_functions=True,
           preserve_classes=True,
           include_docstrings=True,
           include_imports=True
         )
       - Convert splitter output to CodeChunk objects with chunk_type (function/class/module)
       - Include metadata: file_path, function_name, class_name, line_range
    3. split_directory(directory: str, extensions: list = [".py"]) -> List[CodeChunk]:
       - Recursively scan for matching files
       - Call split_file on each
       - Return all chunks flattened
    
    Important: DO NOT use RecursiveCharacterTextSplitter - must use python-semantic-splitter for AST-aware chunking
  </action>
  <verify>python -c "from src.rag.ingest import split_file; chunks = split_file('src/gateway/config.py'); print(f'{len(chunks)} chunks, first: {chunks[0].chunk_type}')"</verify>
  <done>split_file returns CodeChunks with chunk_type 'function'/'class'/'module', not arbitrary splits</done>
</task>

</tasks>

<verification>
- [ ] python-semantic-splitter installed and importable
- [ ] CodeChunk dataclass exported from src.rag
- [ ] split_file() splits at function/class boundaries, not arbitrary character limits
- [ ] split_directory() recursively processes multiple files
- [ ] Chunks include metadata: file_path, function_name, class_name, line_range
</verification>

<success_criteria>
RAG-01: Code files can be ingested and split at function/class boundaries (not arbitrary text splits)
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag-pipeline/03-01-SUMMARY.md`
</output>