---
phase: 03-rag-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/rag/index.py, src/rag/cache.py]
autonomous: true

must_haves:
  truths:
    - "FAISS vector index stores and retrieves code embeddings"
    - "Embeddings are cached and not recomputed on repeated ingestion"
  artifacts:
    - path: "src/rag/index.py"
      provides: "FAISS vector index for code embeddings"
      contains: "CodeIndex, build_index(), add_chunks(), search()"
    - path: "src/rag/cache.py"
      provides: "Embedding cache to avoid recomputation"
      contains: "EmbeddingCache, get(), set()"
  key_links:
    - from: "src/rag/index.py"
      to: "src/rag/ingest"
      via: "import CodeChunk"
      pattern: "from src.rag.ingest import"
    - from: "src/rag/index.py"
      to: "faiss"
      via: "IndexFlatIP"
      pattern: "faiss.IndexFlatIP"
    - from: "src/rag/index.py"
      to: "sentence-transformers"
      via: "SentenceTransformer"
      pattern: "SentenceTransformer"
---

<objective>
Create FAISS vector index with embedding caching for code embeddings

Purpose: Enable efficient similarity search over code chunks with normalized vectors for cosine similarity
Output: src/rag/index.py (FAISS index), src/rag/cache.py (embedding cache)
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rag-pipeline/03-01-PLAN.md
@.planning/phases/03-rag-pipeline/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create embedding cache for repeated code</name>
  <files>src/rag/cache.py</files>
  <action>
    Create src/rag/cache.py with:
    
    1. EmbeddingCache class:
       - __init__(cache_dir: str = ".rag_cache")
       - Uses SHA-256 hash of code text as cache key
       - Stores embeddings as pickle files in cache_dir
       - get(text: str) -> np.ndarray | None (returns cached or None)
       - set(text: str, embedding: np.ndarray) -> None
       - clear() -> None (clear all cached embeddings)
    
    2. Design: File-based cache using pickle, hashlib for key generation
    3. Directory: .rag_cache/ created automatically
  </action>
  <verify>python -c "from src.rag.cache import EmbeddingCache; c = EmbeddingCache(); c.set('test', __import__('numpy').array([0.1]*384)); print(c.get('test') is not None)"</verify>
  <done>EmbeddingCache can store and retrieve embeddings by code text hash</done>
</task>

<task type="auto">
  <name>Task 2: Implement FAISS vector index with normalized embeddings</name>
  <files>src/rag/index.py</files>
  <action>
    Create src/rag/index.py with:
    
    1. CodeIndex class:
       - __init__(model_name: str = "sentence-transformers/all-MiniLM-L6-v2")
       - Uses sentence-transformers with all-MiniLM-L6-v2
       - Creates FAISS IndexFlatIP (inner product for cosine similarity with normalized vectors)
       - EmbeddingCache instance for avoiding recomputation
    
    2. build_index(chunks: List[CodeChunk]) -> None:
       - For each chunk, check cache first (get from EmbeddingCache)
       - If not cached, encode with model (normalize_embeddings=True)
       - Store in cache if newly computed
       - Add to FAISS index
       - Keep mapping: index -> CodeChunk
    
    3. search(query: str, k: int = 3) -> List[CodeChunk]:
       - Encode query with normalize_embeddings=True
       - Search FAISS index for top-k results
       - Return corresponding CodeChunks
    
    4. save_index(path: str) / load_index(path: str) for persistence
    
    IMPORTANT: Must use normalize_embeddings=True for cosine similarity via IndexFlatIP
  </action>
  <verify>python -c "
from src.rag.index import CodeIndex
from src.rag.ingest import split_file
chunks = split_file('src/gateway/config.py')
index = CodeIndex()
index.build_index(chunks)
results = index.search('configuration settings', k=2)
print(f'Found {len(results)} results')
"</verify>
  <done>CodeIndex builds FAISS index, searches return relevant code chunks</done>
</task>

</tasks>

<verification>
- [ ] EmbeddingCache stores/retrieves embeddings by code hash
- [ ] FAISS IndexFlatIP created with 384 dimensions (MiniLM output)
- [ ] Embeddings normalized (normalize_embeddings=True) for cosine similarity
- [ ] Cached embeddings reused on subsequent build_index calls
- [ ] search() returns top-k relevant CodeChunks
</verification>

<success_criteria>
RAG-02: FAISS vector index stores and retrieves code embeddings
RAG-04: Embeddings are cached and not recomputed on repeated ingestion of the same code
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag-pipeline/03-02-SUMMARY.md`
</output>