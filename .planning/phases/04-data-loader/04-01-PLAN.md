---
phase: 04-data-loader
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/neo4j/loader.py
  - src/neo4j/__init__.py
autonomous: true

must_haves:
  truths:
    - "Scraped data loads into Neo4j knowledge graph"
    - "Satellite, Product, Document nodes created from scraped JSON"
    - "FAQs load correctly as query nodes"
  artifacts:
    - path: "src/neo4j/loader.py"
      provides: "Bulk data loading to Neo4j"
      min_lines: 60
    - path: "data/raw/*.json"
      provides: "Source data files"
      contains: "satellites.json"
  key_links:
    - from: "src/neo4j/loader.py"
      to: "src/neo4j/client.py"
      via: "Neo4jClient"
      pattern: "Neo4jClient"
    - from: "src/neo4j/loader.py"
      to: "data/raw/"
      via: "json.load"
      pattern: "json\\.load"
---

<objective>
Create data loader to bulk import scraped JSON data into Neo4j.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
src/neo4j/client.py
data/raw/satellites.json
data/raw/products.json
data/raw/documents.json
data/raw/faqs.json
</context>

<tasks>

<task type="auto">
  <name>Create Neo4j DataLoader class</name>
  <files>src/neo4j/loader.py</files>
  <action>
Create src/neo4j/loader.py with DataLoader class:

1. __init__(client: Neo4jClient) - Store client reference
2. load_satellites(path: str) -> int - Load satellites.json using MERGE on name, label Satellite
3. load_products(path: str) -> int - Load products.json using MERGE on name, label Product
4. load_documents(path: str) -> int - Load documents.json using MERGE on doc_id, label Document
5. load_faqs(path: str) -> int - Load faqs.json using MERGE on faq_id, label FAQ
6. load_all(data_dir: str) -> dict - Batch load all files, return counts

Use Neo4jClient.execute_write() with MERGE for duplicate prevention.
Map JSON fields to node properties: name -> name, url -> url, category -> category, etc.
Log progress: "Loaded N satellites" after each batch.
Return count of nodes created.
  </action>
  <verify>
python -c "from src.neo4j.loader import DataLoader; print('OK')"
  </verify>
  <done>
DataLoader can load all scraped data types into Neo4j
</done>
</task>

<task type="auto">
  <name>Export DataLoader from neo4j package</name>
  <files>src/neo4j/__init__.py</files>
  <action>
Update src/neo4j/__init__.py to export DataLoader:
  from .loader import DataLoader

Add docstring example showing usage.
  </action>
  <verify>
python -c "from src.neo4j import DataLoader; print('OK')"
  </verify>
  <done>
DataLoader exportable from src.neo4j
</done>
</task>

</tasks>

<verification>
- DataLoader imports without errors
- Can load satellites, products, documents, FAQs
- Returns count of nodes created
</verification>

<success_criteria>
- DataLoader loads all scraped data into Neo4j
- Creates nodes with correct labels
- Handles duplicates via MERGE
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-loader/{phase}-01-SUMMARY.md`
</output>