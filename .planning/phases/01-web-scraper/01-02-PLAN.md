---
phase: 01-web-scraper
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scraper/mosdac.py
  - data/raw/satellites.json
  - data/raw/sensors.json
  - data/raw/products.json
  - data/raw/faqs.json
  - data/raw/documents.json
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Scraped data saved as structured JSON with single array [{...}]"
    - "Scraper returns empty list when no data found (no hardcoded fallbacks)"
    - "Dynamic content captured via Selenium for JavaScript-rendered pages"
  artifacts:
    - path: "src/scraper/mosdac.py"
      provides: "MOSDACScraper with fixed JSON serialization, no fallbacks, Selenium enabled"
    - path: "data/raw/*.json"
      contains: "Single array [{}] not double-nested [[{}]]"
  key_links:
    - from: "src/scraper/mosdac.py"
      to: "data/raw/"
      via: "_save_json method"
      pattern: "json.dump.*data.*f"
---

<objective>
Fix 3 gaps from VERIFICATION.md: JSON double-nesting, hardcoded fallbacks, unused Selenium.

Purpose: Ensure scraper produces real data (not stub), proper JSON format, and uses Selenium for dynamic content.
Output: Fixed scraper class and regenerated JSON files.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-web-scraper/01-01-VERIFICATION.md
@src/scraper/mosdac.py

# Gap Details from VERIFICATION.md

## Gap 1: JSON Double-Nesting
- All 5 JSON files contain [[{...}]] instead of [{...}]
- Issue in _save_json or data wrapping
- Fix: Ensure single-level array output

## Gap 2: Hardcoded Fallback Data
- Scraper returns hardcoded defaults when no data found (lines 171-180, 224-233, 282-297, 359-367, 411-420)
- Fix: Remove fallback returns, let scraper return empty list

## Gap 3: Selenium Never Used
- _selenium_get exists but use_selenium=True never passed
- Fix: Call _get() with use_selenium=True for dynamic pages
</context>

<tasks>

<task type="auto">
  <name>Fix JSON serialization to output single-nested arrays</name>
  <files>src/scraper/mosdac.py</files>
  <action>
    In src/scraper/mosdac.py, fix the _save_json method (around line 424-441):
    - Ensure data is passed as-is to json.dump (not wrapped in another list)
    - The current json.dump(data, f) should work, but verify data is a list not a list of lists
    
    Also verify all scrape_* methods return plain lists, not nested lists.
    Example fix: Ensure scrape_satellites() returns `satellites` (a list), not `[[satellites]]`.
  </action>
  <verify>
    Run: python -c "import json; data = json.load(open('data/raw/satellites.json')); print(type(data), len(data))"
    Expected: <class 'list'>, length is item count (not 1 with nested list)
  </verify>
  <done>
    JSON files contain [{...}] not [[{...}]]
  </done>
</task>

<task type="auto">
  <name>Remove hardcoded fallback data returns</name>
  <files>src/scraper/mosdac.py</files>
  <action>
    In src/scraper/mosdac.py, remove all default/fallback data that returns when no scraping occurs:
    
    1. Lines 171-180: Remove default INSAT-3D satellite in scrape_satellites()
    2. Lines 224-233: Remove default sensor in scrape_sensors()  
    3. Lines 282-297: Remove default products list in scrape_products()
    4. Lines 359-367: Remove default FAQ in scrape_faqs()
    5. Lines 411-420: Remove default document in scrape_documents()
    
    After removal, methods should return empty list [] when no data found, not hardcoded data.
    This ensures real scraping occurs or failure is visible (not masked by fallback data).
  </action>
  <verify>
    Run: python -c "from src.scraper import MOSDACScraper; s = MOSDACScraper(); result = s.scrape_satellites(); print('Empty:', len(result) == 0)"
    Expected: True when no real data found (or actual data if scraping works)
  </verify>
  <done>
    Scraper returns empty list when no data found, not hardcoded defaults
  </done>
</task>

<task type="auto">
  <name>Invoke Selenium for dynamic content pages</name>
  <files>src/scraper/mosdac.py</files>
  <action>
    In src/scraper/mosdac.py, enable Selenium for pages that likely require JavaScript rendering:
    
    In each scrape method, identify URLs that need JavaScript and call _get(url, use_selenium=True):
    
    1. scrape_satellites(): Try some URLs with use_selenium=True (e.g., main page with JS-rendered satellite galleries)
    2. scrape_sensors(): Same - use Selenium for sensor pages with dynamic loading
    3. scrape_products(): Use Selenium for product catalogs with JS
    4. scrape_faqs(): Use Selenium for FAQ pages with expandable sections
    5. scrape_documents(): Use Selenium for document listings
    
    Add at least one URL attempt with use_selenium=True in each method to capture dynamic content.
  </action>
  <verify>
    Run: python -c "from src.scraper import MOSDACScraper; s = MOSDACScraper(); print('Selenium available:', s._selenium_get is not None)"
    Expected: True (Selenium method exists and can be called)
  </verify>
  <done>
    _get() called with use_selenium=True for dynamic content pages
  </done>
</task>

<task type="auto">
  <name>Regenerate JSON output files with fixed scraper</name>
  <files>
    data/raw/satellites.json
    data/raw/sensors.json
    data/raw/products.json
    data/raw/faqs.json
    data/raw/documents.json
  </files>
  <action>
    Run the fixed scraper to regenerate JSON files:
    
    ```python
    from src.scraper import MOSDACScraper
    with MOSDACScraper() as scraper:
        outputs = scraper.scrape_all()
        for name, path in outputs.items():
            print(f"Saved: {path}")
    ```
    
    Verify output contains valid JSON with single-nested arrays.
  </action>
  <verify>
    Run: python -c "
import json
for f in ['satellites','sensors','products','faqs','documents']:
    data = json.load(open(f'data/raw/{f}.json'))
    print(f'{f}: {type(data).__name__}, len={len(data)}, first_item_type={type(data[0]).__name__ if data else \"empty\"}')"
    Expected: All are list type, len varies, first item is dict (not list)
  </verify>
  <done>
    All 5 JSON files regenerated with correct single-nested format
  </done>
</task>

</tasks>

<verification>
Gap closure verification:
1. JSON files: Check data/raw/*.json - should be [{...}] not [[{...}]]
2. No fallbacks: grep for default data patterns - should not exist
3. Selenium used: grep "use_selenium=True" in mosdac.py - should have calls

Run final verification:
python -c "
import json
files = ['satellites','sensors','products','faqs','documents']
for f in files:
    data = json.load(open(f'data/raw/{f}.json'))
    assert isinstance(data, list), f'{f} not a list'
    if data:
        assert isinstance(data[0], dict), f'{f} first item not dict'
print('All gaps closed: JSON format correct, no double-nesting')
"
</verification>

<success_criteria>
- JSON files contain single array [{...}] not double-nested [[{...}]]
- No hardcoded fallback data in scraper (returns empty list on failure)
- Selenium called with use_selenium=True for dynamic content
- All 5 JSON files regenerated with correct format
</success_criteria>

<output>
After completion, create `.planning/phases/01-web-scraper/01-02-SUMMARY.md`
</output>