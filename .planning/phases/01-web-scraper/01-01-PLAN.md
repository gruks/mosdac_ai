---
phase: 01-web-scraper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/scraper/__init__.py, src/scraper/mosdac.py, src/scraper/config.py]
autonomous: true
user_setup:
  - service: mosdac.gov.in
    why: "Weather data source"
    env_vars:
      - name: MOSDAC_BASE_URL
        source: "https://www.mosdac.gov.in"
    dashboard_config:
      - task: "No setup needed - public website"

must_haves:
  truths:
    - "Static HTML pages scraped via BeautifulSoup"
    - "Dynamic content captured via Selenium (JavaScript-rendered)"
    - "Scraped data saved as structured JSON"
  artifacts:
    - path: "src/scraper/mosdac.py"
      provides: "MOSDACScraper class for web scraping"
      min_lines: 100
    - path: "data/raw/*.json"
      provides: "Scraped satellite/sensor/product data"
  key_links:
    - from: "src/scraper/mosdac.py"
      to: "data/raw/"
      via: "JSON export"
      pattern: "json.dump"
---

<objective>
Build web scraper for mosdac.gov.in - extract weather data (satellites, sensors, products, FAQs)

Purpose: Collect structured data from MOSDAC website for knowledge graph population
Output: JSON files with scraped data + scraper module
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md (SCRAPE-01 to SCRAPE-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scraper project structure</name>
  <files>src/scraper/__init__.py, src/scraper/config.py</files>
  <action>
Create src/scraper/ package:

1. src/scraper/__init__.py - Package init with MOSDACScraper export
2. src/scraper/config.py - Configuration:
   - MOSDAC_BASE_URL = "https://www.mosdac.gov.in"
   - OUTPUT_DIR = "data/raw"
   - Request timeout, retry settings
   
Use pydantic-settings for configuration.
  </action>
  <verify>python -c "from src.scraper import MOSDACScraper; print('ok')" 2>&1 | grep -q ok</verify>
  <done>Scraper package created with configuration</done>
</task>

<task type="auto">
  <name>Task 2: Build satellite/sensor scraper</name>
  <files>src/scraper/mosdac.py</files>
  <action>
Create src/scraper/mosdac.py with MOSDACScraper class:

1. scrape_satellites() → List[dict]:
   - Scrape /satellite page or equivalent
   - Extract: name, launch_date, agency, description
   - Example: {"name": "INSAT-3D", "launch_date": "2013-01-01", "agency": "ISRO"}

2. scrape_sensors() → List[dict]:
   - Extract sensor name, type, specifications
   - Link to parent satellite

3. Use BeautifulSoup for static HTML
4. Use Selenium for JavaScript-rendered content
5. Add rate limiting (respect robots.txt)

Reference: MOSDAC website structure
  </action>
  <verify>python -c "
from src.scraper.mosdac import MOSDACScraper
scraper = MOSDACScraper()
# Don't actually scrape (no network), just verify class loads
print('MOSDACScraper class loaded')
"</verify>
  <done>MOSDACScraper can fetch satellite/sensor data</done>
</task>

<task type="auto">
  <name>Task 3: Build product/FAQ scraper</name>
  <files>src/scraper/mosdac.py</files>
  <action>
Extend MOSDACScraper:

1. scrape_products() → List[dict]:
   - Extract data products (Temperature, Imagery, etc.)
   - Category, description, applications
   - Example: {"name": "Temperature Data", "category": " Atmospheric", "applications": ["Weather Forecasting"]}

2. scrape_faqs() → List[dict]:
   - Extract Q&A pairs from FAQ pages
   - Questions and answers about data access

3. scrape_documents() → List[dict]:
   - Extract document metadata (user guides, manuals)
   - Title, URL, type, description

4. Save all to data/raw/ as JSON files:
   - satellites.json
   - sensors.json
   - products.json
   - faqs.json
   - documents.json
  </action>
  <verify>grep -n "scrape_products\|scrape_faqs\|scrape_documents" src/scraper/mosdac.py</verify>
  <done>Can scrape products, FAQs, and documents</done>
</task>

</tasks>

<verification>
- [ ] src/scraper/ package created
- [ ] MOSDACScraper class with scrape methods
- [ ] BeautifulSoup + Selenium for different content types
- [ ] Data saved as JSON to data/raw/
- [ ] Rate limiting implemented
</verification>

<success_criteria>
SCRAPE-01 to SCRAPE-05: Weather data from mosdac.gov.in scraped and saved as structured JSON
</success_criteria>

<output>
After completion, create `.planning/phases/01-web-scraper/01-01-SUMMARY.md`
</output>