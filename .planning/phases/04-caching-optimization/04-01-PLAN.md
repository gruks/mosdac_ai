---
phase: 04-caching-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/cache.py
  - src/api/v1/chat.py
autonomous: true

must_haves:
  truths:
    - "Identical requests return cached responses from Redis without hitting the model"
    - "Cache key includes model, messages, temperature, max_tokens, and API key for tenant isolation"
    - "Cache hits are logged with metrics for monitoring hit rate"
  artifacts:
    - path: "src/core/cache.py"
      provides: "ResponseCache class with get/set methods using SHA256 hash keys"
      min_lines: 50
    - path: "src/api/v1/chat.py"
      provides: "Cache lookup before proxy call, cache storage after response"
      contains: "ResponseCache"
  key_links:
    - from: "src/api/v1/chat.py"
      to: "src/core/cache.py"
      via: "import and instantiate ResponseCache"
      pattern: "from src.core.cache import"
    - from: "src/core/cache.py"
      to: "src/core/redis.py"
      via: "get_redis_client()"
      pattern: "get_redis_client"
---

<objective>
Implement Redis exact-match response caching for identical requests.

Purpose: Bypass model inference entirely for repeated queries, reducing latency and GPU load.
Output: Cache module + integration in chat endpoint.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-caching-optimization/04-RESEARCH.md
@src/core/redis.py
@src/api/v1/chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResponseCache class</name>
  <files>src/core/cache.py</files>
  <action>
Create src/core/cache.py with ResponseCache class:
- Accept redis_client via constructor
- Implement _make_key(request_data) using hashlib.sha256 on normalized JSON (sort_keys=True)
- Include in cache key: model, messages, temperature, max_tokens, top_p, seed, and api_key (tenant isolation)
- get() method returns cached response or None
- set() method stores response with TTL (default 3600s = 1 hour)
- Use redis.setex() for atomic set-with-TTL

Reference: Redis exact-match caching pattern from 04-RESEARCH.md
  </action>
  <verify>
grep -n "class ResponseCache" src/core/cache.py && grep -n "_make_key" src/core/cache.py</verify>
  <done>ResponseCache class with get/set methods exists, uses SHA256 hash keys, includes tenant isolation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate cache into chat endpoint</name>
  <files>src/api/v1/chat.py</files>
  <action>
Modify src/api/v1/chat.py chat_completions endpoint:
- Import ResponseCache from src.core.cache
- Create ResponseCache instance using get_redis_client() from src.core.redis
- Before proxy call: build cache key from request_body + token, check cache.get()
- If cache hit: return cached response directly (skip proxy call)
- After successful proxy response: cache.set() the response
- Handle streaming: on cache hit, stream tokens from cached full response to maintain SSE contract

IMPORTANT: Cache key must include token (API key) for tenant isolation.
  </action>
  <verify>
grep -n "ResponseCache" src/api/v1/chat.py && grep -n "cache.get" src/api/v1/chat.py</verify>
  <done>Chat endpoint checks cache before proxy call, returns cached response on hit, stores on miss</done>
</task>

<task type="auto">
  <name>Task 3: Add cache metrics logging</name>
  <files>src/core/cache.py</files>
  <action>
Add logging for cache hits and misses in ResponseCache:
- On cache hit: log at INFO level "cache_hit key={hash_prefix}"
- On cache miss: log at DEBUG level "cache_miss"
- Include cache hit rate tracking if possible

This enables monitoring cache effectiveness via logs.
  </action>
  <verify>
grep -n "cache_hit\|cache_miss" src/core/cache.py</verify>
  <done>Cache operations logged for hit/miss monitoring</done>
</task>

</tasks>

<verification>
- [ ] src/core/cache.py exists with ResponseCache class
- [ ] chat.py imports and uses ResponseCache
- [ ] Cache lookup happens before proxy call
- [ ] Cache includes tenant isolation (API key in key)
- [ ] Streaming cache hits handled correctly (replay as stream)
</verification>

<success_criteria>
Identical requests (same model, messages, parameters, API key) return cached responses without calling Ollama. Cache hit rate is observable via logs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-caching-optimization/04-01-SUMMARY.md`
</output>