---
phase: 02-nlp-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nlp/__init__.py
  - src/nlp/config.py
  - src/nlp/entities.py

autonomous: true

must_haves:
  truths:
    - "User can extract satellite entities (INSAT-3D, INSAT-3DR, etc.) from scraped text"
    - "User can extract sensor entities (Imager, Sounder) from scraped text"
    - "User can extract product entities (weather data products) from scraped text"
  artifacts:
    - path: "src/nlp/config.py"
      provides: "Entity labels (SATELLITE, SENSOR, PRODUCT) and relation types"
      contains: "ENTITY_LABELS"
    - path: "src/nlp/entities.py"
      provides: "GLiNER-based entity extraction"
      exports: ["extract_entities"]
    - path: "src/nlp/__init__.py"
      provides: "NLP module initialization"
  key_links:
    - from: "src/nlp/entities.py"
      to: "GLiNER model"
      via: "GLiNER.from_pretrained()"
      pattern: "gliner.*predict_entities"
---

<objective>
Set up NLP module with config and GLiNER entity extraction.

Purpose: Create foundation for extracting satellite, sensor, and product entities from scraped MOSDAC data using zero-shot NER.
Output: Entity extraction module ready for testing.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Use research for library guidance
@.planning/phases/02-nlp-extraction/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NLP module structure and config</name>
  <files>src/nlp/__init__.py, src/nlp/config.py</files>
  <action>
Create src/nlp/__init__.py with:
- MOSDACNLPModule class initialization
- Exports for entities, relations, normalize, pipeline

Create src/nlp/config.py with:
- ENTITY_LABELS = ["SATELLITE", "SENSOR", "PRODUCT"]
- RELATION_TYPES = ["PROVIDES", "USES", "LOCATED_AT", "MEASURES"]
- GLiNER model config: "urchade/gliner_medium-v2.1"
- spaCy model: "en_core_web_sm"
- Use os.getenv for any configuration (following Phase 1 decisions)
  </action>
  <verify>
ls src/nlp/ shows __init__.py and config.py with content
  </verify>
  <done>NLP module initialized with entity labels and model configs</done>
</task>

<task type="auto">
  <name>Task 2: Implement GLiNER entity extraction</name>
  <files>src/nlp/entities.py</files>
  <action>
Create src/nlp/entities.py:
- Import GLiNER from gliner library
- Create EntityExtractor class with:
  - __init__: Load GLiNER model from config (use gliner_medium-v2.1)
  - extract_satellites(text): Extract satellite entities (INSAT-3D, SCATSAT-1, etc.)
  - extract_sensors(text): Extract sensor entities (Imager, Sounder)
  - extract_products(text): Extract product entities (weather products)
  - extract_all(text): Extract all entity types at once
- Use ENTITY_LABELS from config
- Return entities with: text, label, start, end (span positions)
- Handle case where model fails gracefully (return empty list)
  </action>
  <verify>
python -c "from src.nlp.entities import EntityExtractor; e = EntityExtractor(); print(e.extract_all('INSAT-3D has Imager sensor'))" returns entities
  </verify>
  <done>Entity extraction extracts SATELLITE, SENSOR, PRODUCT entities from any text</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- Entity labels match config (SATELLITE, SENSOR, PRODUCT)
- GLiNER model loads successfully
- Entity spans include start/end positions
</verification>

<success_criteria>
Entity extraction returns correctly labeled entities with span positions for satellites, sensors, and products
</success_criteria>

<output>
After completion, create `.planning/phases/02-nlp-extraction/02-01-SUMMARY.md`
</output>