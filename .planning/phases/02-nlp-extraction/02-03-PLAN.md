---
phase: 02-nlp-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/nlp/pipeline.py

autonomous: true

must_haves:
  truths:
    - "User can run combined pipeline to extract entities and relationships from text"
    - "User can process scraped data files to produce structured output for Neo4j"
  artifacts:
    - path: "src/nlp/pipeline.py"
      provides: "Combined NLP extraction pipeline"
      exports: ["MOSDACPipeline", "process_file"]
  key_links:
    - from: "src/nlp/pipeline.py"
      to: "src/nlp/entities.py"
      via: "EntityExtractor"
      pattern: "EntityExtractor\\(\\)"
    - from: "src/nlp/pipeline.py"
      to: "src/nlp/relations.py"
      via: "RelationExtractor"
      pattern: "RelationExtractor\\(\\)"
    - from: "src/nlp/pipeline.py"
      to: "src/nlp/normalize.py"
      via: "EntityNormalizer"
      pattern: "EntityNormalizer\\(\\)"
---

<objective>
Build combined NLP pipeline integrating entity extraction, relationship extraction, and normalization.

Purpose: Create unified pipeline that processes scraped MOSDAC data through all NLP stages and outputs structured entities/relations for Neo4j.
Output: Complete pipeline ready for data loader phase.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nlp-extraction/02-RESEARCH.md
@.planning/phases/01-web-scraper/*-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified NLP pipeline</name>
  <files>src/nlp/pipeline.py</files>
  <action>
Create src/nlp/pipeline.py:
- Import all components: EntityExtractor, RelationExtractor, EntityNormalizer
- Create MOSDACPipeline class with:
  - __init__: Initialize all three extractors
  - process_text(text): Full pipeline - extract entities, normalize, extract relations
  - process_file(filepath): Read JSON from data/raw/, process all entries
  - Output format: {entities: [...], relations: [...], normalized: {...}}
- Normalize extracted entities before relation extraction
- Only create relations between normalized entities
- Handle errors gracefully (log and continue)
  </action>
  <verify>
python -c "from src.nlp.pipeline import MOSDACPipeline; p = MOSDACPipeline(); print(p.process_text('INSAT-3D provides Imager sensor for weather data'))" returns structured output
  </verify>
  <done>Pipeline processes text through entity extraction, normalization, and relationship extraction</done>
</task>

<task type="auto">
  <name>Task 2: Process scraped data files and save output</name>
  <files>src/nlp/pipeline.py</files>
  <action>
Extend pipeline with:
- process_all_scraped_data(): Process all JSON files in data/raw/
- Save output to data/nlp/ with structured format:
  - data/nlp/entities.json: All extracted and normalized entities
  - data/nlp/relations.json: All extracted relationships
  - data/nlp/combined.json: Full output with metadata
- Output format per entity: {text, label, normalized, source_file, confidence}
- Output format per relation: {subject, object, relation_type, source}
- Create data/nlp/ directory if not exists
  </action>
  <verify>
python -c "from src.nlp.pipeline import process_all_scraped_data; process_all_scraped_data()" creates data/nlp/*.json files
  </verify>
  <done>Scraped data processed and saved to data/nlp/ for Neo4j loading</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- Pipeline integrates all components
- Output format compatible with Neo4j schema (will be defined in Phase 3)
- All scraped data files processed
</verification>

<success_criteria>
Pipeline produces structured entities and relationships from scraped data, saved to data/nlp/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/02-nlp-extraction/02-03-SUMMARY.md`
</output>