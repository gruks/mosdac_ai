---
phase: 06-graphrag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graphrag/__init__.py
  - src/graphrag/retriever.py
  - src/graphrag/vector_search.py
  - src/graphrag/graph_traversal.py
autonomous: true

must_haves:
  truths:
    - "User query can retrieve relevant documents via vector similarity"
    - "User query can traverse graph relationships for relational context"
    - "Vector retriever integrates with Neo4j vector index"
    - "Graph retriever executes multi-hop Cypher queries"
  artifacts:
    - path: "src/graphrag/__init__.py"
      provides: "GraphRAG module exports"
    - path: "src/graphrag/retriever.py"
      provides: "Hybrid retriever combining vector + graph"
    - path: "src/graphrag/vector_search.py"
      provides: "Vector similarity search using neo4j-graphrag"
    - path: "src/graphrag/graph_traversal.py"
      provides: "Graph traversal for satellite-sensor-product relationships"
  key_links:
    - from: "src/graphrag/vector_search.py"
      to: "src/neo4j/client.py"
      via: "Neo4j driver initialization"
    - from: "src/graphrag/graph_traversal.py"
      to: "src/neo4j/client.py"
      via: "execute_read() for Cypher queries"
---

<objective>
Set up vector search and graph traversal retrievers for the GraphRAG pipeline.

Purpose: Enable dual-stream retrieval combining semantic similarity (vector) with relational context (graph). Uses neo4j-graphrag library per research findings.
Output: Vector and graph retriever modules ready for context combination.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-graphrag/06-RESEARCH.md

# Prior work (for patterns and conventions):
@src/neo4j/client.py
@src/rag/index.py
@src/llm/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GraphRAG module with vector search retriever</name>
  <files>src/graphrag/__init__.py, src/graphrag/vector_search.py</files>
  <action>
Create src/graphrag/ directory with __init__.py and vector_search.py.

1. In src/graphrag/__init__.py:
   - Export VectorRetriever class
   - Export HybridRetriever class (will be created in retriever.py)

2. In src/graphrag/vector_search.py:
   - Import os.getenv for config (per project patterns)
   - Use neo4j-graphrag library (neo4j_graphrag.retrievers.VectorRetrieverNeo4j or HybridRetriever)
   - Use SentenceTransformerEmbeddings with model "all-MiniLM-L6-v2" (per research)
   - Initialize with existing Neo4j client from src.neo4j.client
   - Create vector search method: search(query: str, top_k: int) returning list of results
   - Handle missing driver gracefully with error logging

Key reference from research:
- Use neo4j-graphrag>=1.0.0
- Use sentence-transformers>=2.7.0
- Vector index name: "document_embeddings" (assume exists or create dynamically)
  </action>
  <verify>
- python -c "from src.graphrag.vector_search import VectorRetriever; print('VectorRetriever imported')"
- Verify imports neo4j and neo4j_graphrag
  </verify>
  <done>
VectorRetriever can search Neo4j vector index and return relevant documents.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create graph traversal retriever for relationship queries</name>
  <files>src/graphrag/graph_traversal.py</files>
  <action>
Create src/graphrag/graph_traversal.py for multi-hop graph queries.

1. Import from src.neo4j.client import Neo4jClient
2. Create GraphRetriever class with:
   - __init__(neo4j_client: Neo4jClient)
   - traverse(query: str, depth: int = 3) method for multi-hop traversal
   - get_satellite_products(satellite_name: str, top_k: int) for specific relationship queries
   
3. Implement Cypher templates from research:
   - Multi-hop: MATCH (sat:Satellite)-[:PROVIDES]->(sensor:Sensor)-[:MEASURES]->(param:Parameter)
   - Product lookup: MATCH (sensor)-[:PROVIDES]->(product:Product)
   
4. Always specify depth limits to prevent traversal explosion (per research pitfall #2):
   - Use MATCH (a)-[:PROVIDES*1..3]->(b) pattern
   
5. Return structured results with entity names, relationships, and metadata
6. Use os.getenv for any configuration
  </action>
  <verify>
- python -c "from src.graphrag.graph_traversal import GraphRetriever; print('GraphRetriever imported')"
- Verify Cypher query patterns use parameterized queries for security
  </done>
  <done>
GraphRetriever can traverse graph relationships and return satellite-sensor-product paths.
  </done>
</task>

</tasks>

<verification>
- [ ] Both retrievers can import without errors
- [ ] Vector search uses neo4j-graphrag library
- [ ] Graph traversal has depth limits in Cypher queries
- [ ] Both use os.getenv for configuration (per project patterns)
</verification>

<success_criteria>
Two separate retriever modules created:
- VectorRetriever: searches Neo4j vector index for semantic similarity
- GraphRetriever: traverses relationships with multi-hop Cypher queries
</success_criteria>

<output>
After completion, create `.planning/phases/06-graphrag/06-01-SUMMARY.md`
</output>