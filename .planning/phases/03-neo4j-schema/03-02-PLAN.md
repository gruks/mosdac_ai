---
phase: 03-neo4j-schema
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/neo4j/schema.py]
autonomous: true

must_haves:
  truths:
    - "Unique constraints exist for Satellite.name, Sensor.sensor_id, Product.product_id, Document.url, FAQ.faq_id"
    - "Indexes exist on frequently queried properties (type, status, category)"
    - "Schema setup can be run programmatically"
  artifacts:
    - path: "src/neo4j/schema.py"
      provides: "Schema setup with constraints and indexes"
      contains: "create_constraints", "create_indexes"
  key_links:
    - from: "src/neo4j/schema.py"
      to: "src/neo4j/client.py"
      via: "import"
      pattern: "from.*client import"
---

<objective>
Create schema setup module to create constraints and indexes in Neo4j database.

Purpose: Ensure data integrity (unique constraints) and query performance (indexes).
Output: src/neo4j/schema.py with create_constraints() and create_indexes() functions
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-neo4j-schema/03-RESEARCH.md
@.planning/phases/03-neo4j-schema/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema setup module</name>
  <files>src/neo4j/schema.py</files>
  <action>
Create src/neo4j/schema.py with SchemaSetup class:

class SchemaSetup:
  CONSTRAINTS = [
    # Unique constraints (SCHEMA-03)
    "CREATE CONSTRAINT satellite_name_unique IF NOT EXISTS FOR (s:Satellite) REQUIRE s.name IS UNIQUE",
    "CREATE CONSTRAINT sensor_id_unique IF NOT EXISTS FOR (s:Sensor) REQUIRE s.sensor_id IS UNIQUE",
    "CREATE CONSTRAINT product_id_unique IF NOT EXISTS FOR (p:Product) REQUIRE p.product_id IS UNIQUE",
    "CREATE CONSTRAINT document_url_unique IF NOT EXISTS FOR (d:Document) REQUIRE d.url IS UNIQUE",
    "CREATE CONSTRAINT faq_id_unique IF NOT EXISTS FOR (f:FAQ) REQUIRE f.faq_id IS UNIQUE",
  ]
  
  INDEXES = [
    # Query performance indexes (SCHEMA-04)
    "CREATE INDEX satellite_type_idx IF NOT EXISTS FOR (s:Satellite) ON (s.type)",
    "CREATE INDEX satellite_status_idx IF NOT EXISTS FOR (s:Satellite) ON (s.status)",
    "CREATE INDEX sensor_type_idx IF NOT EXISTS FOR (s:Sensor) ON (s.type)",
    "CREATE INDEX product_category_idx IF NOT EXISTS FOR (p:Product) ON (p.category)",
    "CREATE INDEX document_type_idx IF NOT EXISTS FOR (d:Document) ON (d.type)",
    "CREATE INDEX faq_category_idx IF NOT EXISTS FOR (f:FAQ) ON (f.category)",
  ]

  Methods:
  - __init__(client: Neo4jClient)
  - create_constraints(): Run all constraint Cypher, handle "already exists" gracefully
  - create_indexes(): Run all index Cypher, handle "already exists" gracefully
  - setup(): Run both constraints and indexes, return summary dict
  - verify(): Return list of created constraints/indexes

Use IF NOT EXISTS for idempotent runs. Log progress clearly.
  </action>
  <verify>
python -c "from src.neo4j.schema import SchemaSetup; from src.neo4j import Neo4jClient; c = Neo4jClient(); s = SchemaSetup(c); print('OK')"</verify>
  <done>SchemaSetup can be instantiated with Neo4jClient</done>
</task>

<task type="auto">
  <name>Task 2: Add schema module exports</name>
  <files>src/neo4j/__init__.py</files>
  <action>
Update src/neo4j/__init__.py to export SchemaSetup:

from .client import Neo4jClient
from .nodes import Satellite, Sensor, Product, Document, FAQ
from .relationships import PROVIDES, USES, MEASURES, LOCATED_AT, ANSWERS
from .schema import SchemaSetup

__all__ = [
  'Neo4jClient',
  'Satellite', 'Sensor', 'Product', 'Document', 'FAQ',
  'PROVIDES', 'USES', 'MEASURES', 'LOCATED_AT', 'ANSWERS',
  'SchemaSetup',
]
  </action>
<verify>
python -c "from src.neo4j import SchemaSetup; print('OK')"</verify>
  <done>SchemaSetup exported from src.neo4j package</done>
</task>

</tasks>

<verification>
- SchemaSetup class created with CONSTRAINTS and INDEXES
- All 5 unique constraints defined
- All 6 indexes defined
- Exports work from src.neo4j package
</verification>

<success_criteria>
- Unique constraints created for all node primary keys
- Indexes created for frequently queried properties
- setup() runs without errors (idempotent)
- Follows Cypher 5.x syntax from RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-neo4j-schema/03-02-SUMMARY.md`
</output>