---
phase: 03-neo4j-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/neo4j/__init__.py, src/neo4j/client.py, src/neo4j/nodes.py, src/neo4j/relationships.py]
autonomous: true
user_setup:
  - service: neo4j
    why: "Knowledge graph database"
    env_vars:
      - name: NEO4J_URI
        source: "Neo4j Desktop → Select Database → Connect → URI"
      - name: NEO4J_USER
        source: "Default: neo4j"
      - name: NEO4J_PASSWORD
        source: "Neo4j Desktop → Select Database → Connect → Password"

must_haves:
  truths:
    - "Neo4j client can connect to database"
    - "Node types (Satellite, Sensor, Product, Document, FAQ) are defined in code"
    - "Relationship types (PROVIDES, USES, MEASURES, LOCATED_AT, ANSWERS) are defined in code"
  artifacts:
    - path: "src/neo4j/client.py"
      provides: "Neo4j driver wrapper with connection management"
      min_lines: 50
    - path: "src/neo4j/nodes.py"
      provides: "Node type definitions with properties"
      contains: "class Satellite", "class Sensor", "class Product", "class Document", "class FAQ"
    - path: "src/neo4j/relationships.py"
      provides: "Relationship type definitions"
      contains: "PROVIDES", "USES", "MEASURES", "LOCATED_AT", "ANSWERS"
  key_links:
    - from: "src/neo4j/client.py"
      to: "src/neo4j/nodes.py"
      via: "import"
      pattern: "from.*nodes import"
    - from: "src/neo4j/client.py"
      to: "src/neo4j/relationships.py"
      via: "import"
      pattern: "from.*relationships import"
---

<objective>
Create Neo4j schema module with node and relationship type definitions, plus client wrapper.

Purpose: Enable Phase 4 (Data Loader) to write extracted entities to Neo4j knowledge graph.
Output: src/neo4j/ module with client.py, nodes.py, relationships.py
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nlp-extraction/02-01-SUMMARY.md
@.planning/phases/02-nlp-extraction/02-02-SUMMARY.md
@.planning/phases/03-neo4j-schema/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Neo4j client wrapper</name>
  <files>src/neo4j/__init__.py, src/neo4j/client.py</files>
  <action>
Create src/neo4j/ directory with:
- __init__.py: Export Neo4jClient
- client.py: Neo4jClient class using neo4j 5.x driver
  - __init__(uri, user, password): Create GraphDatabase.driver
  - close(): Close driver connection
  - execute_write(query, params): Run write transaction
  - execute_read(query, params): Run read transaction
  - verify_connectivity(): Test connection on init

Use os.getenv for config (per prior phase decisions):
- NEO4J_URI (default: bolt://localhost:7687)
- NEO4J_USER (default: neo4j)
- NEO4J_PASSWORD (required)

Wrap in try/except for connection errors, log clearly.
  </action>
  <verify>
python -c "from src.neo4j import Neo4jClient; c = Neo4jClient(); c.close(); print('OK')"</verify>
  <done>Neo4jClient can be imported and instantiated without errors</done>
</task>

<task type="auto">
  <name>Task 2: Define node type classes</name>
  <files>src/neo4j/nodes.py</files>
  <action>
Create node type classes with properties matching Phase 2 entity extraction:

class Satellite:
  - name (str, primary): INSAT-3D, Kalpana, etc.
  - type (str): Geostationary, LEO
  - launch_date (str): YYYY-MM-DD
  - orbit_type (str): GEO, LEO
  - status (str): Operational, Inactive
  - operator (str): ISRO, etc.

class Sensor:
  - sensor_id (str, primary): imager_1, sounder_1
  - name (str): Imager, Sounder
  - type (str): Multi-spectral, Hyperspectral
  - channels (int): number of channels
  - resolution (str): 1km, 500m

class Product:
  - product_id (str, primary): cloud_product_1
  - name (str): Cloud Products, Temperature Data
  - category (str): Meteorological, Oceanographic
  - format (str): HDF, NetCDF, GIF
  - resolution (str): 1km, 4km

class Document:
  - doc_id (str, primary): doc_001
  - title (str): Technical document title
  - url (str): Source URL
  - type (str): Technical Document, Manual, FAQ
  - source (str): MOSDAC, ISRO

class FAQ:
  - faq_id (str, primary): faq_001
  - question (str): The question text
  - answer (str): The answer text
  - category (str): Data Access, Products, etc.

Include to_dict() methods for Cypher parameter conversion.
  </action>
  <verify>
python -c "from src.neo4j.nodes import Satellite, Sensor, Product, Document, FAQ; print('OK')"</verify>
  <done>All 5 node types can be imported and instantiated</done>
</task>

<task type="auto">
  <name>Task 3: Define relationship type classes</name>
  <files>src/neo4j/relationships.py</files>
  <action>
Create relationship type definitions using Phase 2 extraction:

class PROVIDES:
  - since (str): date relationship started
  - status (str): Active, Inactive
  
class USES:
  - since (str): date relationship started
  
class MEASURES:
  - unit (str): Kelvin, etc.
  - range (str): value range
  
class LOCATED_AT:
  - section (str): page/section reference
  
class ANSWERS:
  - relevance (float): 0-1 score

Include to_cypher() method returning relationship type string.
Include properties_dict() method returning dict for SET clause.
  </action>
  <verify>
python -c "from src.neo4j.relationships import PROVIDES, USES, MEASURES, LOCATED_AT, ANSWERS; print('OK')"</verify>
  <done>All 5 relationship types can be imported and instantiated</done>
</task>

</tasks>

<verification>
- All imports work without errors
- Node classes have to_dict() methods
- Relationship classes have to_cypher() and properties_dict() methods
- Config uses os.getenv (per prior phase pattern)
</verification>

<success_criteria>
- Neo4j client connects to database (if credentials provided)
- Node types match Phase 2 entity extraction output
- Relationship types match Phase 2 relationship extraction output
- Code follows existing project patterns (os.getenv config)
</success_criteria>

<output>
After completion, create `.planning/phases/03-neo4j-schema/03-01-SUMMARY.md`
</output>