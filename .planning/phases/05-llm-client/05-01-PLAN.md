---
phase: 05-llm-client
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/__init__.py
  - src/llm/client.py
autonomous: true

must_haves:
  truths:
    - "LLM client connects to fine-tuned model REST API"
    - "Client sends text queries and receives JSON responses"
    - "API key authentication works via headers"
  artifacts:
    - path: "src/llm/client.py"
      provides: "LLM client wrapper to call REST API"
      min_lines: 50
    - path: "src/llm/__init__.py"
      provides: "Client module exports"
      contains: "LLMClient"
  key_links:
    - from: "src/llm/client.py"
      to: "config"
      via: "API key from env var"
      pattern: "os.getenv.*API_KEY"
    - from: "src/rag/"
      to: "src/llm/client.py"
      via: "import LLMClient"
      pattern: "from src.llm import"
---

<objective>
Create LLM client to call fine-tuned model REST API.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

User provided context:
- REST API type
- Text queries + JSON payloads
- API key authentication required
</context>

<tasks>

<task type="auto">
  <name>Create LLMClient for REST API</name>
  <files>src/llm/client.py</files>
  <action>
Create src/llm/client.py with LLMClient class:

1. __init__(api_key: str, base_url: str) - Store API key and base URL from env vars
2. query(text: str, **kwargs) -> dict - Send text query to API, return JSON response
3. _make_request(method: str, endpoint: str, payload: dict) -> dict - Make HTTP request with auth header

Use requests library. API key in Authorization header: Bearer token.
Read base URL from LLM_API_URL env var (default: from user config).
Log requests: "Sending query to LLM API...", "Response received"
Handle errors: connection errors, auth errors, timeout.
  </action>
  <verify>
python -c "from src.llm.client import LLMClient; print('OK')"
  </verify>
  <done>
LLMClient can query fine-tuned model API with text and return JSON response
  </done>
</task>

<task type="auto">
  <name>Export LLMClient from package</name>
  <files>src/llm/__init__.py</files>
  <action>
Create src/llm/__init__.py:
  from .client import LLMClient

Add docstring with usage example.
  </action>
  <verify>
python -c "from src.llm import LLMClient; print('OK')"
  </verify>
  <done>
LLMClient exportable from src.llm
  </done>
</task>

</tasks>

<verification>
- LLMClient imports without errors
- Can make authenticated requests to REST API
- Returns parsed JSON responses
- Handles errors gracefully
</verification>

<success_criteria>
- LLMClient connects to fine-tuned model REST API
- Sends text queries, receives JSON responses
- API key auth via Bearer token header
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-client/{phase}-01-SUMMARY.md`
</output>