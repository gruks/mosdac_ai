---
phase: 02-api-gateway-multi-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gateway/__init__.py
  - src/gateway/config.py
  - src/core/__init__.py
  - src/core/redis.py
  - src/core/logging.py
  - pyproject.toml
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Application starts without import errors"
    - "Redis connection pool is established and reusable"
    - "Configuration loads from environment with type validation"
    - "Structured logging outputs JSON-formatted log entries"
  artifacts:
    - path: "src/gateway/config.py"
      provides: "Pydantic-settings based configuration with env var loading"
      contains: "class Settings(BaseSettings)"
    - path: "src/core/redis.py"
      provides: "Async Redis client with connection pool"
      exports: ["get_redis_client"]
    - path: "src/core/logging.py"
      provides: "Structured logging configuration"
      exports: ["setup_logging", "get_logger"]
    - path: "pyproject.toml"
      provides: "Project dependencies for gateway"
      contains: "fastapi"
  key_links:
    - from: "src/gateway/config.py"
      to: "environment variables"
      via: "pydantic-settings"
      pattern: "Settings\\(.*_env_file"
    - from: "src/core/redis.py"
      to: "src/gateway/config.py"
      via: "imports REDIS_URL from settings"
      pattern: "from.*config.*import.*settings"
---

<objective>
Create the foundation layer: project structure, dependencies, configuration, Redis connection pool, and structured logging. This is the shared infrastructure that all subsequent plans depend on.

Purpose: Without this layer, auth cannot load API key config, rate limiting cannot connect to Redis, and the proxy cannot read vLLM backend URLs.
Output: Working project skeleton with validated config, Redis client, and logging.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-gateway-multi-model/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure, dependencies, and configuration</name>
  <files>
    pyproject.toml
    .env.example
    src/gateway/__init__.py
    src/gateway/config.py
    src/core/__init__.py
  </files>
  <action>
    Create the project directory structure and core configuration:

    1. Create directory structure:
       - src/gateway/ (main app module)
       - src/api/v1/ (API route handlers)
       - src/core/ (infrastructure: redis, logging)
       - src/tests/ (test suite)
       Each directory gets an __init__.py file.

    2. Create pyproject.toml with dependencies:
       - fastapi>=0.115.0
       - uvicorn[standard]>=0.34.0
       - httpx>=0.28.0
       - slowapi>=0.1.9
       - redis>=5.2.0
       - pydantic>=2.0
       - pydantic-settings>=2.0
       - python-multipart>=0.0.18
       - passlib[bcrypt]>=1.7.4
       - python-dotenv>=1.0.1
       Run: pip install -e .

    3. Create src/gateway/config.py with pydantic-settings:
       - class Settings(BaseSettings) with:
         - redis_url: str = "redis://localhost:6379/0"
         - redis_max_connections: int = 10
         - vllm_backends: dict (model_name -> url mapping, loaded from env as JSON string)
         - default_timeout_connect: float = 5.0
         - default_timeout_read: float = 300.0
         - default_timeout_write: float = 10.0
         - default_timeout_pool: float = 5.0
         - rate_limit_default: str = "100/minute"
         - api_keys_file: str = "data/api_keys.json"
         - log_level: str = "INFO"
       - Use model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")
       - Export singleton: settings = Settings()

    4. Create .env.example with all configurable variables documented.

    Use pydantic-settings (not os.environ scattered) for type validation, defaults, and env file support per research findings.
  </action>
  <verify>
    - python -c "from src.gateway.config import settings; print(settings)" runs without error
    - pip list shows all dependencies installed
    - Directory structure matches: src/gateway/, src/api/v1/, src/core/, src/tests/
  </verify>
  <done>
    - pyproject.toml exists with all required dependencies
    - src/gateway/config.py exports Settings class with all fields
    - settings loads from environment or uses defaults
    - .env.example documents all configuration variables
    - All package __init__.py files exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up Redis connection pool and structured logging</name>
  <files>
    src/core/redis.py
    src/core/logging.py
  </files>
  <action>
    Create the Redis client and logging infrastructure:

    1. Create src/core/redis.py:
       - Async Redis client using redis.asyncio.Redis
       - Connection pool with max_connections from settings.redis_max_connections
       - Function: async def get_redis_client() -> redis.asyncio.Redis
       - Lazy initialization: create pool on first call, reuse thereafter
       - Include a health check function: async def check_redis_health() -> bool
       - Import settings from src.gateway.config

    2. Create src/core/logging.py:
       - Function: def setup_logging(level: str = "INFO") -> None
       - Configure JSON-formatted structured logging using Python's built-in logging module
       - Include: timestamp, level, logger name, message, optional extra fields
       - Function: def get_logger(name: str) -> logging.Logger
       - Use structlog if available, otherwise use standard logging with JSON formatter
       - Log level from settings.log_level

    Do NOT hand-roll Redis connection management — use redis.asyncio.Redis with from_url() and connection pool. Do NOT use basic print() or unstructured logging.
  </action>
  <verify>
    - python -c "from src.core.redis import get_redis_client; print(type(get_redis_client()))" runs without error
    - python -c "from src.core.logging import setup_logging, get_logger; setup_logging(); logger = get_logger('test'); logger.info('test')" outputs JSON-formatted log
    - Redis health check returns False when Redis is not running (expected, not an error)
  </verify>
  <done>
    - src/core/redis.py exports get_redis_client and check_redis_health
    - src/core/logging.py exports setup_logging and get_logger
    - Redis client uses async connection pool from redis-py
    - Logging outputs structured JSON format
    - Both modules import settings from config without circular imports
  </done>
</task>

</tasks>

<verification>
- [ ] pip install -e . succeeds with all dependencies
- [ ] from src.gateway.config import settings works, settings has all expected fields
- [ ] from src.core.redis import get_redis_client works, returns async Redis client
- [ ] from src.core.logging import setup_logging, get_logger works, outputs JSON logs
- [ ] Directory structure: src/gateway/, src/api/v1/, src/core/, src/tests/ all exist with __init__.py
- [ ] .env.example exists with documented variables
</verification>

<success_criteria>
- Project structure is created with all required directories and __init__.py files
- All dependencies are installed and importable
- Settings class loads configuration from environment variables with sensible defaults
- Redis async client is creatable with connection pooling
- Structured logging is configured and outputs JSON format
- No circular import errors between config, redis, and logging modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-gateway-multi-model/02-01-SUMMARY.md`
</output>
