---
phase: 01-core-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference/config/vllm-config.yaml
  - inference/scripts/start-vllm.sh
  - inference/docker/Dockerfile.vllm
autonomous: true
user_setup:
  - service: huggingface
    why: "Download Qwen2.5-Coder-32B-Instruct model weights"
    env_vars:
      - name: HF_TOKEN
        source: "HuggingFace -> Settings -> Access Tokens"
    dashboard_config:
      - task: "Accept Qwen2.5-Coder-32B-Instruct model card agreement"
        location: "https://huggingface.co/Qwen/Qwen2.5-Coder-32B-Instruct"
  - service: nvidia-dgx
    why: "GPU drivers and CUDA toolkit must be installed on DGX H200"
    env_vars: []
    dashboard_config:
      - task: "Verify nvidia-smi shows 2+ GPUs with CUDA >= 12.1"
        location: "Run nvidia-smi on the host"

must_haves:
  truths:
    - "vLLM server starts without errors and loads Qwen2.5-Coder-32B-Instruct"
    - "Server listens on configurable host and port (default 0.0.0.0:8000)"
    - "Tensor parallelism is set to 2 for dual-GPU inference"
    - "GPU memory utilization is tuned to prevent OOM (starts at 0.90)"
  artifacts:
    - path: "inference/config/vllm-config.yaml"
      provides: "vLLM server arguments as structured config"
      contains: "tensor-parallel-size"
    - path: "inference/scripts/start-vllm.sh"
      provides: "Executable startup script with correct flags"
      min_lines: 15
    - path: "inference/docker/Dockerfile.vllm"
      provides: "Container definition for reproducible deployment"
      contains: "vllm==0.19.0"
  key_links:
    - from: "inference/scripts/start-vllm.sh"
      to: "inference/config/vllm-config.yaml"
      via: "script reads config file for server arguments"
      pattern: "vllm.*serve.*Qwen.*Coder"
    - from: "inference/scripts/start-vllm.sh"
      to: "GPU hardware"
      via: "tensor-parallel-size 2 + gpu-memory-utilization 0.90"
      pattern: "tensor-parallel-size 2"
---

<objective>
Set up vLLM inference server with Qwen2.5-Coder-32B-Instruct using tensor parallelism (TP=2) and tuned GPU memory utilization.

Purpose: This is the foundation — without a running vLLM server, nothing else in this phase works. The server natively provides `/v1/chat/completions` (INF-01), streaming (INF-02), token counting (INF-03), and max_tokens enforcement (INF-04).

Output: Configuration file, startup script, and Dockerfile that launch a working vLLM instance.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-inference/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vLLM configuration and startup script</name>
  <files>inference/config/vllm-config.yaml
inference/scripts/start-vllm.sh</files>
  <action>Create vLLM configuration file at inference/config/vllm-config.yaml with these server arguments:
- model: Qwen/Qwen2.5-Coder-32B-Instruct
- tensor_parallel_size: 2
- gpu_memory_utilization: 0.90 (start conservative per research — can increase to 0.95 if stable)
- max_model_len: 4096 (realistic for code completion, not 32K — avoids KV cache fragmentation)
- host: 0.0.0.0
- port: 8000
- dtype: auto
- trust_remote_code: true

Create startup script at inference/scripts/start-vllm.sh that:
1. Checks for HF_TOKEN environment variable (exit 1 with helpful message if missing)
2. Checks nvidia-smi is available and reports GPU count (warn if < 2 GPUs)
3. Reads configuration from vllm-config.yaml (parse YAML or use hardcoded defaults from config)
4. Runs `vllm serve Qwen/Qwen2.5-Coder-32B-Instruct` with all flags from config
5. Includes `--enforce-eager` flag is NOT set (use default V1 engine, not V0)
6. Make script executable (chmod +x)

Do NOT hand-roll OpenAI-compatible endpoints, SSE streaming, or token counting — vLLM serve provides all of these natively per research.
</action>
  <verify>
- `cat inference/config/vllm-config.yaml` shows all required parameters
- `bash -n inference/scripts/start-vllm.sh` passes syntax check
- `ls -la inference/scripts/start-vllm.sh` shows executable permission
- Script contains `vllm serve` command with `--tensor-parallel-size 2` and `--gpu-memory-utilization 0.90`
</verify>
  <done>Configuration file exists with all vLLM parameters; startup script is executable, validates prerequisites, and launches vLLM with correct flags</done>
</task>

<task type="auto">
  <name>Task 2: Create Dockerfile for vLLM server</name>
  <files>inference/docker/Dockerfile.vllm</files>
  <action>Create Dockerfile at inference/docker/Dockerfile.vllm that:
1. Uses `nvidia/cuda:12.1.0-base-ubuntu22.04` as base image (CUDA 12.1 is vLLM 0.19.0 requirement)
2. Installs Python 3.10+ and pip
3. Installs `vllm==0.19.0` via pip
4. Copies config and startup script into container
5. Sets HF_TOKEN as build arg (not hardcoded)
6. Exposes port 8000
7. Sets ENTRYPOINT to the startup script
8. Includes health check instruction: `HEALTHCHECK --interval=30s --timeout=10s --retries=3 CMD curl -f http://localhost:8000/health || exit 1`

Note: The /health endpoint will be provided by the FastAPI sidecar (Plan 02). The Dockerfile healthcheck can use a basic curl to port 8000 for now — it will work once the sidecar is running on the same port or proxied.
</action>
  <verify>
- `docker build -f inference/docker/Dockerfile.vllm --no-cache .` completes without errors (if Docker available)
- Dockerfile contains `vllm==0.19.0` pin
- Dockerfile exposes port 8000
- Dockerfile references the startup script as entrypoint
</verify>
  <done>Dockerfile exists, pins vLLM 0.19.0, exposes port 8000, and uses the startup script as entrypoint</done>
</task>

</tasks>

<verification>
- Both files exist and are syntactically valid
- vLLM config contains all 7 required parameters
- Startup script checks HF_TOKEN and GPU availability before launching
- Dockerfile pins vLLM version and exposes correct port
</verification>

<success_criteria>
- inference/config/vllm-config.yaml exists with tensor_parallel_size=2, gpu_memory_utilization=0.90, max_model_len=4096
- inference/scripts/start-vllm.sh is executable and contains vllm serve command with all flags
- inference/docker/Dockerfile.vllm exists with vLLM 0.19.0 pinned
- All INF-06 requirements satisfied (Qwen2.5-Coder-32B with TP=2 configured)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-inference/01-01-SUMMARY.md`
</output>
