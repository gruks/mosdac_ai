---
phase: 01-core-inference
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - inference/scripts/test-inference.py
  - inference/scripts/verify-setup.sh
autonomous: true

must_haves:
  truths:
    - "Developer can POST to /v1/chat/completions and receive a code completion response"
    - "Response streams tokens in real-time when stream: true is set"
    - "Response includes prompt_tokens, completion_tokens, and total_tokens in usage object"
    - "max_tokens parameter correctly limits response length"
    - "GET /health returns healthy status with GPU metrics"
  artifacts:
    - path: "inference/scripts/test-inference.py"
      provides: "Smoke test script covering all INF requirements"
      min_lines: 80
    - path: "inference/scripts/verify-setup.sh"
      provides: "End-to-end verification script"
      min_lines: 20
  key_links:
    - from: "inference/scripts/test-inference.py"
      to: "http://localhost:8000/v1/chat/completions"
      via: "HTTP POST with code completion prompt"
      pattern: "chat/completions"
    - from: "inference/scripts/test-inference.py"
      to: "http://localhost:8001/health"
      via: "HTTP GET for health check"
      pattern: "health"
---

<objective>
Create smoke tests and verification scripts that prove all Phase 1 requirements (INF-01 through INF-06) are met.

Purpose: Without tests, we cannot confirm the vLLM server and health sidecar actually work. These scripts validate streaming, token counting, max_tokens enforcement, and health metrics — the observable behaviors from the phase goal.

Output: Python smoke test script and shell verification script.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-inference/01-01-PLAN.md
@.planning/phases/01-core-inference/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python smoke test for all inference requirements</name>
  <files>inference/scripts/test-inference.py</files>
  <action>Create inference/scripts/test-inference.py — a standalone Python script (no test framework needed, just requests/httpx + sys) that:

1. Accepts optional --base-url (default http://localhost:8000) and --health-url (default http://localhost:8001/health) arguments
2. Tests INF-05 (health endpoint first — fastest failure):
   - GET /health, verify status 200
   - Assert response has keys: status, model, tensor_parallel_size, vllm_reachable, gpus
   - Assert gpus is non-empty list with utilization, memory_used_mb, memory_total_mb per GPU
   - Print: "PASS: Health endpoint returns GPU metrics"
3. Tests INF-01 (basic completion):
   - POST /v1/chat/completions with body: {"model": "Qwen/Qwen2.5-Coder-32B-Instruct", "messages": [{"role": "user", "content": "Write a Python function that adds two numbers"}], "max_tokens": 50, "stream": false}
   - Verify status 200
   - Assert response has choices[0].message.content (non-empty string)
   - Assert response has usage.prompt_tokens, usage.completion_tokens, usage.total_tokens
   - Print: "PASS: Basic code completion works"
4. Tests INF-02 (streaming):
   - POST /v1/chat/completions with same body but "stream": true
   - Verify response is SSE (Content-Type contains text/event-stream or chunks arrive incrementally)
   - Collect all chunks, verify final chunk contains "[DONE]" or equivalent termination
   - Verify at least one chunk has non-empty content
   - Print: "PASS: Streaming responses work"
5. Tests INF-04 (max_tokens enforcement):
   - POST /v1/chat/completions with "max_tokens": 10
   - Verify response usage.completion_tokens <= 10 (allow small tolerance of +2 for tokenizer variance)
   - Print: "PASS: max_tokens enforced"
6. Prints summary: total tests, passed, failed
7. Exits with code 0 if all pass, 1 if any fail

Use httpx (not requests) for async streaming support. Wrap each test in try/except with clear error messages. Do NOT require GPU to run — if vLLM is not running, print clear "vLLM not reachable at {url}" message and exit.
</action>
  <verify>
- `python -c "import ast; ast.parse(open('inference/scripts/test-inference.py').read())"` passes syntax check
- Script contains tests for: health endpoint, basic completion, streaming, max_tokens
- Script uses httpx for HTTP calls
- Script accepts --base-url and --health-url CLI arguments
- Script exits 0 on all pass, 1 on any failure
- Script has clear PASS/FAIL output for each test
</verify>
  <done>Smoke test script exists, covers INF-01/02/04/05, uses httpx, accepts CLI args, prints per-test PASS/FAIL, exits with appropriate code</done>
</task>

<task type="auto">
  <name>Task 2: Create shell verification wrapper</name>
  <files>inference/scripts/verify-setup.sh</files>
  <action>Create inference/scripts/verify-setup.sh that:
1. Prints "=== Phase 1: Core Inference Verification ==="
2. Checks vLLM server is running: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health` — expect 200
3. Checks health sidecar is running: `curl -s http://localhost:8001/health | python -m json.tool` — expect valid JSON with "status" key
4. Runs the Python smoke test: `python inference/scripts/test-inference.py`
5. Prints summary: "Phase 1 verification: PASS" or "Phase 1 verification: FAIL — see errors above"
6. Make script executable (chmod +x)

This is a convenience wrapper — the real testing is done by test-inference.py. The shell script just checks services are up before running the full test suite.
</action>
  <verify>
- `bash -n inference/scripts/verify-setup.sh` passes syntax check
- `ls -la inference/scripts/verify-setup.sh` shows executable permission
- Script checks both port 8000 (vLLM) and port 8001 (gateway)
- Script runs test-inference.py as the comprehensive test
</verify>
  <done>Verification script exists, is executable, checks both services are running, and runs the full smoke test suite</done>
</task>

</tasks>

<verification>
- test-inference.py covers INF-01 (completion), INF-02 (streaming), INF-04 (max_tokens), INF-05 (health)
- INF-03 (token counting) is verified within the INF-01 test (checks usage object)
- INF-06 (Qwen2.5-Coder-32B with TP=2) is verified by health endpoint response (tensor_parallel_size: 2, model name)
- verify-setup.sh checks service availability before running tests
- Both scripts are executable and syntactically valid
</verification>

<success_criteria>
- inference/scripts/test-inference.py tests all 6 INF requirements
- inference/scripts/verify-setup.sh provides one-command verification
- Running verify-setup.sh with vLLM + gateway running produces all PASS results
- Each test has clear PASS/FAIL output with actionable error messages
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-inference/01-03-SUMMARY.md`
</output>
