---
phase: 04-caching-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gateway/config.py
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Ollama keep_alive is set to '1h' for KV cache reuse on shared system prompts"
    - "OLLAMA_NUM_PARALLEL is configured to 2-4 to prevent GPU OOM under concurrent load"
    - "GPU memory utilization stays within 85-90% target under sustained load"
  artifacts:
    - path: "src/gateway/config.py"
      provides: "Cache TTL and keep_alive configuration settings"
      contains: "cache_ttl"
    - path: ".env.example"
      provides: "Documentation of required OLLAMA environment variables"
      contains: "OLLAMA_KEEP_ALIVE"
  key_links:
    - from: "src/gateway/config.py"
      to: "Ollama server"
      via: "Environment variables passed to Ollama process"
      pattern: "OLLAMA_KEEP_ALIVE"
---

<objective>
Configure Ollama keep_alive and GPU memory tuning for KV cache optimization.

Purpose: Enable Ollama's built-in KV cache by keeping models loaded, and limit concurrent requests to maintain 85-90% GPU memory utilization.
Output: Config settings + documentation.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-caching-optimization/04-RESEARCH.md
@src/gateway/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cache configuration to settings</name>
  <files>src/gateway/config.py</files>
  <action>
Add to src/gateway/config.py Settings class:
- cache_ttl: int = Field(default=3600, description="Response cache TTL in seconds")
- ollama_keep_alive: str = Field(default="1h", description="Ollama keep_alive duration for KV cache")
- ollama_num_parallel: int = Field(default=2, description="Max concurrent requests to prevent OOM")

These settings control cache behavior at the application level.
  </action>
  <verify>
grep -n "cache_ttl\|ollama_keep_alive\|ollama_num_parallel" src/gateway/config.py</verify>
  <done>Settings class includes cache_ttl, ollama_keep_alive, ollama_num_parallel configuration</done>
</task>

<task type="auto">
<parameter name>Task 2: Update .env.example with Ollama variables</name>
<files>.env.example</files>
  <action>
Update .env.example to document required Ollama server-side configuration:

```
# Ollama Server Configuration (set in systemd service or shell environment)
# Keep model loaded in GPU for KV cache reuse (default: 5m)
OLLAMA_KEEP_ALIVE=1h

# Limit concurrent requests to prevent OOM (2-4 for 4GB GPU)
OLLAMA_NUM_PARALLEL=2

# Optional: Set GPU memory utilization target (handled by Ollama automatically)
# OLLAMA_GPU_MEMORY_UTILIZATION=0.85
```

Add these under an "OLLAMA SERVER CONFIGURATION" section.
  </action>
  <verify>
grep -n "OLLAMA_KEEP_ALIVE\|OLLAMA_NUM_PARALLEL" .env.example</verify>
  <done>.env.example documents Ollama server-side configuration for KV cache and OOM prevention</done>
</task>

<task type="auto">
  <name>Task 3: Document configuration requirements</name>
  <files>.planning/phases/04-caching-optimization/04-02-SUMMARY.md</files>
  <action>
Create 04-02-SUMMARY.md with configuration instructions:

1. For Ollama running as systemd service: Add Environment=OLLAMA_KEEP_ALIVE=1h to /etc/systemd/system/ollama.service
2. For Ollama running manually: Export OLLAMA_KEEP_ALIVE=1h before starting
3. Set OLLAMA_NUM_PARALLEL=2 (or 3-4 based on available VRAM)

This ensures the three-tier caching works:
- Tier 1: Exact-match (Redis) - bypasses model entirely
- Tier 2: Ollama KV cache - model stays loaded, processes shared prefix faster
- Tier 3: Fresh inference
  </action>
  <verify>
cat .planning/phases/04-caching-optimization/04-02-SUMMARY.md | head -30</verify>
  <done>Ollama server configuration documented in SUMMARY for user setup</done>
</task>

</tasks>

<verification>
- [ ] Settings include cache_ttl, ollama_keep_alive, ollama_num_parallel
- [ ] .env.example documents OLLAMA_KEEP_ALIVE and OLLAMA_NUM_PARALLEL
- [ ] Configuration supports three-tier caching architecture
</verification>

<success_criteria>
Ollama keeps models loaded for 1 hour (KV cache reuse), concurrent requests limited to 2 to prevent GPU OOM, GPU memory stays within 85-90% target.
</success_criteria>

<output>
After completion, update `.planning/phases/04-caching-optimization/04-02-SUMMARY.md` with full configuration details
</output>